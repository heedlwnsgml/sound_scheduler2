name: sound_scheduler2
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  # 사용자의 SDK 버전을 유지합니다
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- 7개 오류의 원인이었던 'localizations'만 제거 ---
  # flutter_localizations:
  #   sdk: flutter
  permission_handler: ^11.3.1
  sound_mode: ^3.1.1
  android_alarm_manager_plus: ^4.0.0
  shared_preferences: ^2.2.3

  # 'intl'은 날짜 포맷('11월 17일 (월)')을 위해 남겨둡니다.
  intl: ^0.20.2
  # --- --- ---

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true